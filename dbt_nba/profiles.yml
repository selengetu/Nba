# Uses MOTHERDUCK_TOKEN env var. Run from project root or set DBT_PROFILES_DIR.
nba:
  target: prod
  outputs:
    prod:
      type: duckdb
      path: "md:{{ env_var('MOTHERDUCK_DATABASE', 'nba') }}"
      schema: "{{ env_var('DBT_TARGET_SCHEMA', 'analytics') }}"
      threads: 4
